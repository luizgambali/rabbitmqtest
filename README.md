Exemplo simples de utilização do RabbitMQ com C#

Antes de executar o projeto, rodar o docker compose com o arquivo yaml na pasta docker.
Na pasta raiz do projeto:

docker-compose -f ./docker/rabbitmq-compose.yaml up

Ao executar o projeto, os dois apps console serão executados simultaneamente.
